# Boring Vectors

An SVG editor and maker for the web. See `CLAUDE.md` and `docs/SPEC.md` for the brief and build plan.

## Dev

```
npm install
npm run dev
```

## Stack

Vite + React 18 + TypeScript, Paper.js for the canvas scene graph, Zustand for UI state.
